# 영어 시간표 Enrollment 통합 분석 보고서

**날짜**: 2026-01-10
**작성자**: Claude (AI Assistant)
**목적**: 영어 시간표와 새 enrollment 구조 통합 방안 분석

---

## 📋 목차

1. [문제 요약](#문제-요약)
2. [현재 상태 분석](#현재-상태-분석)
3. [데이터 흐름 비교](#데이터-흐름-비교)
4. [해결 방안](#해결-방안)
5. [구현 계획](#구현-계획)
6. [리스크 분석](#리스크-분석)

---

## 문제 요약

### ✅ 수학 시간표: 정상 작동
- **데이터 소스**: `students/{studentId}/enrollments` (새 구조)
- **작동 방식**: `useEnrollmentsAsClasses` 훅으로 enrollment 기반 데이터 로드
- **학생 배정**: `AssignClassModal`로 배정 시 즉시 시간표에 반영됨

### ❌ 영어 시간표: 문제 발생
- **데이터 소스**: `수업목록` 컬렉션 + `CLASS_COLLECTION` (기존 구조)
- **작동 방식**: 고정된 수업 데이터만 표시
- **학생 배정**: `AssignClassModal`로 배정해도 시간표에 반영 안됨

### 핵심 문제
> `AssignClassModal`은 `students/{studentId}/enrollments`에만 데이터를 저장하지만,
> 영어 시간표는 `수업목록` 컬렉션에서만 학생 목록을 읽어옴

---

## 현재 상태 분석

### 1. 수학 시간표 아키텍처

```
┌─────────────────────────────────────────────────────────────┐
│ AssignClassModal (학생 배정)                                 │
└────────────┬────────────────────────────────────────────────┘
             │
             ▼
  students/{studentId}/enrollments/{enrollmentId}
  {
    subject: 'math',
    className: '중등E_초6 SKY',
    teacherId: '학성쌤',
    schedule: ['월-1', '수-1', '금-1']
  }
             │
             ▼
┌────────────────────────────────────────────────────────────┐
│ useEnrollmentsAsClasses('math')                            │
│ - collectionGroup으로 모든 enrollments 조회                 │
│ - className별 그룹화                                        │
│ - TimetableClass[] 반환                                     │
└────────────┬───────────────────────────────────────────────┘
             │
             ▼
┌────────────────────────────────────────────────────────────┐
│ useTimetableClasses() → TimetableManager                   │
│ - 수학 시간표 정상 표시                                      │
│ - 학생 목록 정상 표시                                        │
└────────────────────────────────────────────────────────────┘
```

### 2. 영어 시간표 아키텍처

```
┌─────────────────────────────────────────────────────────────┐
│ AssignClassModal (학생 배정)                                 │
└────────────┬────────────────────────────────────────────────┘
             │
             ▼
  students/{studentId}/enrollments/{enrollmentId}
  {
    subject: 'english',
    className: 'DP_초4 B',
    teacherId: 'Sarah',
    schedule: ['화-3', '목-3']
  }
             │
             ✗ (여기서 끊김 - 영어 시간표가 읽지 못함)


┌────────────────────────────────────────────────────────────┐
│ EnglishTimetable                                           │
│ - EN_COLLECTION에서 스케줄 데이터 읽음                      │
│ - useClassStudents → CLASS_COLLECTION에서 학생 목록 읽음    │
│ - enrollments는 읽지 않음                                   │
└────────────────────────────────────────────────────────────┘
             ▲
             │
  수업목록/{className}  ← 여기서 학생 목록 조회
  {
    className: 'DP_초4 B',
    teacher: 'Sarah',
    studentIds: ['학생1_ID', '학생2_ID']  ← 수동 업데이트 필요
  }
```

---

## 데이터 흐름 비교

### 수학 시간표 데이터 흐름

| 단계 | 컴포넌트/훅 | 데이터 소스 | 결과 |
|------|------------|------------|------|
| 1 | `AssignClassModal` | → `students/{id}/enrollments` 생성 | ✅ |
| 2 | `useEnrollmentsAsClasses('math')` | ← `collectionGroup(enrollments)` | ✅ |
| 3 | `useTimetableClasses` | ← enrollment 기반 클래스 목록 | ✅ |
| 4 | `TimetableManager` | ← 시간표 렌더링 | ✅ |

### 영어 시간표 데이터 흐름

| 단계 | 컴포넌트/훅 | 데이터 소스 | 결과 |
|------|------------|------------|------|
| 1 | `AssignClassModal` | → `students/{id}/enrollments` 생성 | ✅ |
| 2 | `useClassStudents` | ← `수업목록` 컬렉션 | ❌ (enrollments 안 봄) |
| 3 | `EnglishTimetable` | ← 수업목록 기반 렌더링 | ❌ (신규 학생 없음) |

---

## 해결 방안

### 방안 1: AssignClassModal 확장 (단기 해결책) ⭐ 권장

**개요**: 영어 과목 배정 시 `수업목록` 컬렉션도 함께 업데이트

**장점**:
- ✅ 최소한의 코드 변경
- ✅ 기존 영어 시간표 구조 유지
- ✅ 즉시 적용 가능
- ✅ 롤백 쉬움

**단점**:
- ⚠️ 이중 데이터 저장 (enrollments + 수업목록)
- ⚠️ 데이터 일관성 관리 필요

**구현 위치**: `components/StudentManagement/AssignClassModal.tsx`

```typescript
// handleSubmit 함수 수정
const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    // ... 기존 validation

    try {
        const selectedClass = currentClasses?.find(c => c.className === selectedClassName);
        if (!selectedClass) throw new Error('수업을 찾을 수 없습니다');

        const enrollmentId = `enrollment_${Date.now()}`;

        // 1. enrollment 생성 (기존 로직)
        await setDoc(doc(db, `students/${student.id}/enrollments`, enrollmentId), {
            subject: selectedSubject,
            className: selectedClass.className,
            teacherId: selectedClass.teacher,
            schedule: selectedClass.schedule || [],
            days: [],
            period: null,
            room: null,
            startDate: Timestamp.now(),
            endDate: null,
            color: null,
            createdAt: Timestamp.now(),
        });

        // 2. 영어 과목인 경우 수업목록도 업데이트 (신규 로직)
        if (selectedSubject === 'english') {
            const classDocRef = doc(db, '수업목록', selectedClass.className);
            const classSnap = await getDoc(classDocRef);

            if (classSnap.exists()) {
                const currentData = classSnap.data();
                const currentStudentIds = currentData.studentIds || [];

                // 중복 방지
                if (!currentStudentIds.includes(student.id)) {
                    await updateDoc(classDocRef, {
                        studentIds: [...currentStudentIds, student.id],
                        updatedAt: Timestamp.now()
                    });
                }
            } else {
                // 수업목록 문서가 없으면 생성
                await setDoc(classDocRef, {
                    className: selectedClass.className,
                    teacher: selectedClass.teacher,
                    subject: 'english',
                    studentIds: [student.id],
                    schedule: selectedClass.schedule || [],
                    createdAt: Timestamp.now(),
                    updatedAt: Timestamp.now()
                });
            }
        }

        onSuccess();
        onClose();
    } catch (err) {
        console.error('수업 배정 오류:', err);
        setError('수업 배정 중 오류가 발생했습니다');
    }
};
```

**필요한 import 추가**:
```typescript
import { getDoc, updateDoc } from 'firebase/firestore';
```

---

### 방안 2: 영어 시간표 전면 마이그레이션 (장기 해결책)

**개요**: 영어 시간표도 수학처럼 enrollment 기반으로 전환

**장점**:
- ✅ 데이터 구조 통일
- ✅ 단일 진실 공급원 (Single Source of Truth)
- ✅ 향후 유지보수 용이

**단점**:
- ❌ 대규모 코드 변경 필요
- ❌ 영어 시간표 UI 전면 수정 필요
- ❌ 테스트 기간 필요
- ❌ 리스크 높음

**영향받는 파일**:
1. `components/Timetable/English/EnglishTimetable.tsx`
2. `components/Timetable/English/hooks/useClassStudents.ts`
3. `components/Timetable/English/EnglishClassTab.tsx`
4. 기타 영어 시간표 관련 컴포넌트 다수

---

## 구현 계획

### Phase 1: 방안 1 구현 (단기)

#### Step 1: AssignClassModal 수정
- [ ] `getDoc`, `updateDoc` import 추가
- [ ] `handleSubmit` 함수에 영어 과목 처리 로직 추가
- [ ] 에러 핸들링 추가

#### Step 2: 테스트
- [ ] 영어 수업 배정 테스트
- [ ] 수업목록 컬렉션 업데이트 확인
- [ ] 영어 시간표에서 학생 표시 확인

#### Step 3: 학생 삭제 처리
- [ ] 학생 퇴원/삭제 시 `수업목록`에서도 제거되도록 수정
- [ ] `components/StudentManagement/StudentDetail.tsx` 수정 필요

### Phase 2: 데이터 일관성 스크립트 (선택)

```typescript
// scripts/sync-english-enrollments-to-class-collection.ts
/**
 * enrollments에는 있지만 수업목록에는 없는 영어 학생들을 동기화
 */
async function syncEnglishEnrollmentsToClassCollection() {
    const enrollmentsQuery = query(
        collectionGroup(db, 'enrollments'),
        where('subject', '==', 'english')
    );

    const snapshot = await getDocs(enrollmentsQuery);

    const classStudents = new Map<string, string[]>();

    // enrollments에서 className별 studentIds 수집
    snapshot.docs.forEach(doc => {
        const data = doc.data();
        const studentId = doc.ref.parent.parent?.id || '';
        const className = data.className;

        if (!classStudents.has(className)) {
            classStudents.set(className, []);
        }
        classStudents.get(className)!.push(studentId);
    });

    // 수업목록 업데이트
    for (const [className, studentIds] of classStudents) {
        const classDocRef = doc(db, '수업목록', className);
        const classSnap = await getDoc(classDocRef);

        if (classSnap.exists()) {
            const currentIds = classSnap.data().studentIds || [];
            const mergedIds = Array.from(new Set([...currentIds, ...studentIds]));

            await updateDoc(classDocRef, {
                studentIds: mergedIds,
                updatedAt: Timestamp.now()
            });

            console.log(`✅ ${className}: ${mergedIds.length}명`);
        }
    }
}
```

---

## 리스크 분석

### 방안 1 리스크

| 리스크 | 영향도 | 발생 확률 | 완화 방안 |
|--------|--------|-----------|-----------|
| 데이터 불일치 | 중 | 중 | 동기화 스크립트 작성 |
| 성능 저하 (이중 쓰기) | 낮 | 낮 | 배정 빈도가 높지 않음 |
| 삭제 시 orphan 데이터 | 중 | 중 | 삭제 로직도 함께 수정 |

### 방안 2 리스크

| 리스크 | 영향도 | 발생 확률 | 완화 방안 |
|--------|--------|-----------|-----------|
| 대규모 버그 발생 | 높음 | 중 | 충분한 테스트 기간 |
| UI 호환성 문제 | 높음 | 높음 | 점진적 마이그레이션 |
| 데이터 손실 | 높음 | 낮음 | 백업 필수 |

---

## 권장 사항

### 즉시 조치 (방안 1)

1. **AssignClassModal 수정** (30분 소요)
   - 영어 과목 배정 시 `수업목록` 컬렉션 업데이트
   - 에러 핸들링 추가

2. **테스트** (1시간 소요)
   - 영어 수업 배정 → 시간표 확인
   - 여러 학생 배정 테스트
   - 중복 배정 방지 확인

3. **삭제 로직 수정** (30분 소요)
   - 학생 퇴원 시 `수업목록`에서도 제거

### 향후 계획 (방안 2)

1. **요구사항 분석** (2주)
   - 영어 시간표 현재 기능 파악
   - enrollment 기반 전환 시 필요 기능

2. **설계** (1주)
   - 데이터 모델 설계
   - UI/UX 설계

3. **구현 & 테스트** (4주)
   - 단계별 마이그레이션
   - 충분한 테스트

---

## 코드 체크리스트

### 수정 필요 파일

- [ ] `components/StudentManagement/AssignClassModal.tsx`
  - handleSubmit 함수 수정
  - import 추가

- [ ] `components/StudentManagement/StudentDetail.tsx` (선택)
  - 학생 삭제/퇴원 시 수업목록 업데이트

- [ ] `scripts/sync-english-enrollments.ts` (선택)
  - 기존 데이터 동기화 스크립트

### 테스트 체크리스트

- [ ] 영어 수업 신규 배정 → 시간표 표시 확인
- [ ] 수학 수업 배정 → 정상 작동 확인 (regression test)
- [ ] 동일 수업 중복 배정 방지 확인
- [ ] 학생 퇴원 시 수업목록에서 제거 확인

---

## 결론

**단기 해결책 (방안 1)** 을 우선 구현하여 즉시 문제를 해결하고,
**장기적으로 방안 2**를 통해 데이터 구조를 통일하는 것을 권장합니다.

현재 시점에서는:
1. **AssignClassModal 수정**으로 영어 시간표 문제 해결
2. **동기화 스크립트**로 기존 데이터 정리
3. **삭제 로직 수정**으로 데이터 일관성 유지

위 3가지를 완료하면 영어 시간표가 정상적으로 작동하며,
향후 여유가 생겼을 때 방안 2를 통해 구조를 개선할 수 있습니다.

---

**보고서 끝**
