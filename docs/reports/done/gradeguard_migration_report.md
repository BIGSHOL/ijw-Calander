# GradeGuard 이식 검토 보고서

**작성일**: 2026-01-09
**작성자**: Claude Code
**버전**: 1.1 (구현 방향 확정)

---

## 1. 개요

GradeGuard 프로젝트(d:\gradeguard)의 성적 관리 기능을 ijw-calander 프로젝트로 이식하는 방안에 대한 검토 보고서입니다.

---

## 2. GradeGuard 현재 구조 분석

### 2.1 파일 구조
```
gradeguard/
├── App.tsx              # 메인 앱 (525줄)
├── types.ts             # 타입 정의 (59줄)
├── index.tsx            # 엔트리 포인트
├── components/
│   ├── Charts.tsx       # Recharts 기반 차트 (93줄)
│   ├── Sidebar.tsx      # 사이드바 네비게이션
│   └── StatCard.tsx     # 통계 카드 컴포넌트
├── services/
│   └── mockData.ts      # 목데이터 (54줄)
├── package.json         # 의존성
└── vite.config.ts       # Vite 설정
```

### 2.2 주요 기능
| 기능 | 설명 | 구현 상태 |
|------|------|----------|
| 대시보드 | 학생별 성적 종합 분석 | ✅ 구현됨 |
| 성적 차트 | 시험별 점수 추이 차트 | ✅ 구현됨 |
| 학생 목록 | 필터링/검색 기능 | ✅ 구현됨 |
| 학생 프로필 | 출석률, 반 정보 표시 | ✅ 구현됨 |
| 시험 일정 | 다가오는 시험 표시 | ✅ 구현됨 |
| 리더보드 | 학교별 우수 학생 | ✅ 구현됨 |
| 새 성적 입력 | CRUD 기능 | ❌ 미구현 (alert만) |
| 리포트 다운로드 | PDF/Excel 내보내기 | ❌ 미구현 |

### 2.3 데이터 모델 (types.ts)
```typescript
// 핵심 타입들
enum Subject { MATH, ENGLISH, KOREAN, SCIENCE }
enum GradeLevel { MIDDLE_1~3, HIGH_1~3 }

interface Student {
  id, name, avatar, school, gradeLevel, className, attendanceRate
}

interface Score {
  examId, examTitle, subject, score, average, maxScore, grade
}

interface StudentStats {
  student, currentGpa, totalAverage, scores[], rankInClass, totalStudentsInClass
}

interface UpcomingEvent {
  id, title, date, subject, dDay
}
```

### 2.4 사용 라이브러리
- **recharts**: 차트 라이브러리 (ComposedChart, Line, Bar)
- **lucide-react**: 아이콘 (ijw-calander와 동일)
- **vite**: 빌드 도구 (ijw-calander와 동일)

---

## 3. ijw-calander 현재 구조

### 3.1 관련 기존 시스템
| 시스템 | 설명 | 연동 가능성 |
|--------|------|------------|
| **Student** | 학생 관리 (students 컬렉션) | ✅ 높음 |
| **Attendance** | 출석 관리 | ✅ 높음 |
| **Teacher** | 강사 관리 | ✅ 높음 |
| **Calendar** | 일정 관리 | 중간 |
| **Timetable** | 시간표 | 중간 |

### 3.2 기존 Student 타입 (ijw-calander)
```typescript
// types.ts에서 발췌
interface Student {
  id, name, school, grade, teacher, subjects, group
  enrollmentDate, withdrawalDate, attendanceDays
}
```

### 3.3 권한 시스템
- `usePermissions` hook 사용
- 역할 기반 접근 제어 (master, admin, manager, editor, viewer)

---

## 4. 이식 방안 비교

### 옵션 A: 독립 탭으로 추가
```
장점:
- 기존 코드 영향 최소화
- 빠른 구현 가능 (1-2주)
- 독립적 테스트 가능

단점:
- 데이터 중복 가능성
- 학생 정보 이중 관리

구현 방식:
- 새 탭 'grades' 추가 (TAB_GROUPS에 등록)
- components/Grades/ 폴더 생성
- 별도 Firestore 컬렉션 (grades, exams, scores)
```

### 옵션 B: 학생 관리 탭 확장
```
장점:
- 기존 학생 데이터 활용
- 일관된 사용자 경험
- 데이터 중복 없음

단점:
- 기존 코드 수정 필요
- 복잡도 증가
- 구현 시간 더 소요 (3-4주)

구현 방식:
- StudentManagementTab 내 서브탭 추가
- 학생 상세 페이지에 성적 섹션 추가
- 기존 Student 타입 확장
```

### ✅ 옵션 C: 하이브리드 접근 (확정)
```
장점:
- 대시보드는 독립 탭 (네비게이션 하단 탭)
- 상세 정보는 학생 관리와 연동
- 유연한 구조
- 간단한 연동으로 빠른 구현 가능

구현 방식:
1. '성적' 탭: 네비게이션 하단에 추가 (전체 대시보드, 분석 기능)
2. StudentDetail: '성적' 하위 탭 추가 (개별 학생 성적 조회/입력)
3. 공통 hooks/services로 데이터 공유
```

---

## 4.1. 확정된 구현 방향 (2026-01-09)

### 1차 목표: 간단한 연동
사용자 요구사항에 따라 다음과 같이 구현합니다:

#### 네비게이션 구조
```
App.tsx 하단 탭 (TAB_GROUPS)
├── 캘린더
├── 학생관리 ← 기존
│   └── StudentDetail
│       ├── 기본정보
│       ├── 수업 (수강정보)
│       ├── 성적 ← NEW! (1차 목표)
│       └── 콜앤상담
├── 출석부
├── 간트차트
└── 성적관리 ← NEW! (2차 목표: 전체 대시보드)
```

#### 학생 성적 탭 (StudentDetail 하위)
```
┌─────────────────────────────────────────────────┐
│  강민승 (Willy)                                 │
├─────────────────────────────────────────────────┤
│  [기본정보] [수업] [성적] [콜앤상담]            │ ← 성적 탭 추가
├─────────────────────────────────────────────────┤
│                                                 │
│  📊 성적 현황                                   │
│  ┌──────────────────────────────────────────┐  │
│  │ 최근 시험: 1월 모의고사 (수학)           │  │
│  │ 점수: 85/100 (반평균: 72)                │  │
│  │ 등급: B+ | 석차: 5/32                    │  │
│  └──────────────────────────────────────────┘  │
│                                                 │
│  📈 성적 추이 차트 (Recharts)                   │
│  [━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━]              │
│                                                 │
│  📋 시험별 성적 목록                            │
│  ┌────────┬──────┬──────┬──────┬──────┐       │
│  │ 시험명 │ 과목 │ 점수 │ 평균 │ 등급 │       │
│  ├────────┼──────┼──────┼──────┼──────┤       │
│  │ 1월모의│ 수학 │  85  │  72  │  B+  │       │
│  │ 12월기말│ 수학│  78  │  70  │  B   │       │
│  └────────┴──────┴──────┴──────┴──────┘       │
│                                                 │
│  [+ 새 성적 입력]                               │
│                                                 │
└─────────────────────────────────────────────────┘
```

---

## 5. 확정 구현 계획 (옵션 C - 간단한 연동)

### 1단계: 학생 성적 탭 추가 (1차 목표)

#### Phase 1: 데이터 모델 설계 (0.5일)
```typescript
// types.ts에 추가
interface Exam {
  id: string;
  title: string;           // "1월 모의고사"
  date: string;            // "2026-01-15"
  type: 'midterm' | 'final' | 'mock' | 'weekly';
  subject: 'math' | 'english';
  gradeLevel?: string;     // "중1", "고2" 등
  createdBy: string;
  createdAt: number;
}

interface StudentScore {
  id: string;
  studentId: string;       // 기존 Student.id와 연결
  examId: string;          // Exam.id와 연결
  score: number;           // 점수 (0-100)
  maxScore: number;        // 만점 (기본 100)
  average?: number;        // 반평균 (선택)
  rank?: number;           // 석차 (선택)
  totalStudents?: number;  // 전체 학생수 (선택)
  grade?: 'A+' | 'A' | 'B+' | 'B' | 'C+' | 'C' | 'D' | 'F';
  memo?: string;           // 메모
  createdAt: number;
  updatedAt: number;
  createdBy: string;
}
```

#### Phase 2: Firebase 컬렉션 설계 (0.5일)
```
Firestore 구조:
├── exams/                    # 시험 정보
│   └── {examId}
│       ├── title, date, type, subject
│       └── createdBy, createdAt
├── student_scores/           # 학생별 성적
│   └── {scoreId}
│       ├── studentId, examId
│       ├── score, maxScore
│       └── average, rank, grade, memo
└── students/                 # 기존 컬렉션 (변경 없음)
```

#### Phase 3: Hooks 개발 (1일)
```typescript
// hooks/useStudentGrades.ts
useStudentScores(studentId)   // 학생별 성적 조회 (핵심)
useAddScore()                 // 성적 입력
useUpdateScore()              // 성적 수정
useDeleteScore()              // 성적 삭제

// hooks/useExams.ts
useExams()                    // 시험 목록 조회
useCreateExam()               // 시험 생성
```

#### Phase 4: GradesTab 컴포넌트 (1일)
```
components/StudentManagement/tabs/
├── GradesTab.tsx             # 성적 탭 (NEW)
│   ├── 성적 현황 요약
│   ├── 성적 추이 차트 (간단한 버전)
│   ├── 시험별 성적 목록
│   └── 새 성적 입력 버튼
└── (기존 파일들)
```

#### Phase 5: StudentDetail 연동 (0.5일)
```typescript
// StudentDetail.tsx 수정
const tabs = [
  { id: 'basic', label: '기본정보', icon: User },
  { id: 'courses', label: '수업', icon: BookOpen },
  { id: 'grades', label: '성적', icon: GraduationCap },  // NEW
  { id: 'consultations', label: '콜앤상담', icon: Phone },
];

// 렌더링
{activeTab === 'grades' && <GradesTab student={student} />}
```

### 2단계: 성적관리 탭 추가 (2차 목표)

#### Phase 6: 독립 탭 추가 (2일)
```
components/Grades/
├── GradesManager.tsx         # 메인 컨테이너
├── GradesDashboard.tsx       # 전체 대시보드
├── GradesChart.tsx           # Recharts 성적 차트
├── ExamManagement.tsx        # 시험 관리
└── ScoreInput.tsx            # 일괄 성적 입력
```

#### Phase 7: TAB_GROUPS 등록 (0.5일)
```typescript
// App.tsx
{ id: 'grades', label: '성적관리', icon: GraduationCap }
```

---

## 5.1 1차 목표 상세 작업 목록

| # | 작업 | 파일 | 예상 소요 | 우선순위 |
|---|------|------|----------|---------|
| 1 | Exam, StudentScore 타입 추가 | types.ts | 0.5h | P0 |
| 2 | useStudentScores hook 개발 | hooks/useStudentGrades.ts | 2h | P0 |
| 3 | useExams hook 개발 | hooks/useExams.ts | 1h | P0 |
| 4 | GradesTab 컴포넌트 생성 | components/StudentManagement/tabs/GradesTab.tsx | 3h | P0 |
| 5 | StudentDetail 탭 추가 | components/StudentManagement/StudentDetail.tsx | 0.5h | P0 |
| 6 | 성적 입력 폼 (간단한 버전) | GradesTab.tsx 내 | 1h | P1 |
| 7 | 성적 차트 (LineChart) | GradesTab.tsx 내 | 1h | P2 |

**1차 목표 예상 총 소요: 약 1일**

---

## 6. 작업 목록

### 즉시 필요
| # | 작업 | 예상 소요 | 우선순위 |
|---|------|----------|---------|
| 1 | 타입 정의 추가 | 0.5일 | P1 |
| 2 | Firebase 컬렉션 설계 | 0.5일 | P1 |
| 3 | useGrades hooks 개발 | 2일 | P1 |
| 4 | GradesManager 컴포넌트 | 2일 | P1 |
| 5 | 차트 컴포넌트 이식 | 1일 | P1 |

### 후속 작업
| # | 작업 | 예상 소요 | 우선순위 |
|---|------|----------|---------|
| 6 | 권한 설정 | 0.5일 | P2 |
| 7 | TAB_GROUPS 등록 | 0.5일 | P2 |
| 8 | 학생 관리 연동 | 2일 | P2 |
| 9 | 리포트 기능 | 3일 | P3 |
| 10 | 모바일 최적화 | 2일 | P3 |

---

## 7. 예상 리스크

| 리스크 | 영향도 | 완화 방안 |
|--------|-------|----------|
| 데이터 마이그레이션 | 중간 | 기존 학생 ID 그대로 사용 |
| recharts 버전 충돌 | 낮음 | ijw-calander에서 이미 사용 중 (확인 필요) |
| 권한 복잡도 증가 | 중간 | 기존 패턴 그대로 적용 |
| 성능 저하 | 낮음 | React Query 캐싱 활용 |

---

## 8. 의존성 확인

### GradeGuard에서 사용 중
```json
{
  "recharts": "^2.x",
  "lucide-react": "^0.x",
  "react": "^18.x"
}
```

### ijw-calander 현재 상태
- **recharts**: 확인 필요 (package.json 검토)
- **lucide-react**: ✅ 사용 중
- **react**: ✅ 18.x 사용 중

---

## 9. 결론 및 권장사항

### 확정된 접근 방식
**옵션 C (하이브리드)** - 학생 성적 탭(1차) + 독립 탭(2차)

### 예상 총 소요 시간
- **1차 목표 (학생 성적 탭)**: 약 1일
- **2차 목표 (전체 대시보드)**: 약 2-3일
- **총합**: 약 3-4일

### 시작 전 확인 사항
1. ✅ recharts 의존성 확인 (package.json)
2. Firestore 보안 규칙 검토 (exams, student_scores 컬렉션)
3. ✅ 기존 Student 타입과의 필드 매핑 확정 (studentId로 연결)

### 우선순위 (업데이트됨)
1. **P0**: 학생 성적 탭 (StudentDetail 하위) - 1차 목표
2. **P1**: 성적 입력 폼
3. **P2**: 성적 차트
4. **P3**: 전체 대시보드 탭 - 2차 목표
5. **P4**: 리포트/내보내기

---

## 10. 구현 현황 (1차 목표 완료)

### 1차 목표 구현 완료: ✅
1. ✅ types.ts에 Exam, StudentScore 타입 추가
2. ✅ hooks/useStudentGrades.ts 생성
3. ✅ hooks/useExams.ts 생성
4. ✅ components/StudentManagement/tabs/GradesTab.tsx 생성
5. ✅ StudentDetail.tsx에 성적 탭 추가

### 구현된 기능:
- **성적 조회**: 학생별 시험 성적 조회 (과목 필터링)
- **성적 입력**: 시험 선택 후 점수, 평균, 석차 입력
- **시험 등록**: 새 시험 생성 (제목, 날짜, 유형, 과목)
- **성적 삭제**: 개별 성적 삭제
- **통계 표시**: 평균 점수, 최고 점수, 추이, 등급 표시
- **차트 시각화**: Recharts LineChart로 성적 추이 표시
- **등급 자동 계산**: 백분율 기반 A+~F 등급 자동 산출

### Firestore 컬렉션:
- `exams` - 시험 정보
- `student_scores` - 학생별 성적

### 2차 목표 (추후 진행):
6. components/Grades/ 폴더 구조 생성
7. GradesManager.tsx 기본 구현
8. TAB_GROUPS에 '성적관리' 탭 등록

---

## 11. 변경 이력

| 버전 | 날짜 | 변경 내용 |
|------|------|----------|
| 1.0 | 2026-01-09 | 초안 작성 |
| 1.1 | 2026-01-09 | 구현 방향 확정 (네비게이션 탭 + 학생 성적 탭) |
| 1.2 | 2026-01-09 | **1차 목표 구현 완료** - 학생 성적 탭 추가 |
