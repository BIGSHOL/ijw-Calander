# 네비게이션 그룹화 드롭다운 개선 계획서

> **작성일**: 2026-01-08
> **우선순위**: P2 (중간)
> **예상 소요**: 1-2주
> **목적**: 늘어나는 탭을 논리적으로 그룹화하여 확장성 확보

---

## 📋 현황 분석

### 현재 탭 구조 (7개 → 8개 예정)

```
[📅 연간 일정] [📋 시간표] [📝 출석부] [💳 전자 결제] [📊 간트 차트] [💬 상담 관리] [👥 학생 관리] [📊 성적 관리(예정)]
```

### 문제점
- 화면 너비에 따라 탭이 잘림
- 10개 이상 시 관리 불가
- 관련 기능 간 연관성 파악 어려움
- 모바일 대응 어려움

---

## 🎯 개선 방향: 그룹화 드롭다운

### 제안 구조

```
[📅 일정]  [📚 수업 ▼]  [👥 학생 ▼]  [⚙️ 관리 ▼]
             │           │            │
             ├─ 시간표    ├─ 학생 관리  ├─ 간트 차트
             └─ 출석부    ├─ 상담 관리  ├─ 전자 결재
                          └─ 성적 관리  └─ (확장 가능)
```

### 그룹 정의

| 그룹 | 아이콘 | 포함 탭 | 설명 |
|------|--------|---------|------|
| **📅 일정** | Calendar | 연간 일정 | 단독 탭 (드롭다운 없음) |
| **📚 수업** | Book | 시간표, 출석부 | 수업 운영 관련 |
| **👥 학생** | Users | 학생 관리, 상담 관리, 성적 관리 | 학생 데이터 관련 |
| **⚙️ 관리** | Settings | 간트 차트, 전자 결재 | 운영/관리 도구 |

---

## 🏗️ 기술 구현 계획

### 1. 데이터 구조 변경

#### 현재 (`types.ts`)
```typescript
export type AppTab = 'calendar' | 'timetable' | 'payment' | 'gantt' | 'consultation' | 'attendance' | 'students';

export const APP_TABS: { id: AppTab; label: string }[] = [
  { id: 'calendar', label: '연간 일정' },
  { id: 'timetable', label: '시간표' },
  // ...
];
```

#### 변경 후
```typescript
export type AppTab = 'calendar' | 'timetable' | 'payment' | 'gantt' | 'consultation' | 'attendance' | 'students' | 'grades';

export interface TabGroup {
  id: string;
  label: string;
  icon: string;
  tabs: AppTab[];
  isDropdown: boolean;
}

export const TAB_GROUPS: TabGroup[] = [
  {
    id: 'schedule',
    label: '일정',
    icon: '📅',
    tabs: ['calendar'],
    isDropdown: false,  // 단독 탭
  },
  {
    id: 'class',
    label: '수업',
    icon: '📚',
    tabs: ['timetable', 'attendance'],
    isDropdown: true,
  },
  {
    id: 'student',
    label: '학생',
    icon: '👥',
    tabs: ['students', 'consultation', 'grades'],
    isDropdown: true,
  },
  {
    id: 'admin',
    label: '관리',
    icon: '⚙️',
    tabs: ['gantt', 'payment'],
    isDropdown: true,
  },
];

// 탭 메타데이터
export const TAB_META: Record<AppTab, { label: string; icon: string }> = {
  calendar: { label: '연간 일정', icon: '📅' },
  timetable: { label: '시간표', icon: '📋' },
  attendance: { label: '출석부', icon: '📝' },
  payment: { label: '전자 결재', icon: '💳' },
  gantt: { label: '간트 차트', icon: '📊' },
  consultation: { label: '상담 관리', icon: '💬' },
  students: { label: '학생 관리', icon: '👥' },
  grades: { label: '성적 관리', icon: '📊' },
};
```

---

### 2. 컴포넌트 구조

#### 신규 컴포넌트
```
components/Navigation/
├── NavigationBar.tsx       # 메인 네비게이션 바
├── TabGroup.tsx            # 드롭다운 그룹
├── TabItem.tsx             # 개별 탭 아이템
└── MobileNav.tsx           # 모바일용 (햄버거 메뉴)
```

#### NavigationBar.tsx 개요
```tsx
const NavigationBar = () => {
  const [openDropdown, setOpenDropdown] = useState<string | null>(null);
  
  return (
    <nav className="flex items-center gap-1 px-4">
      {TAB_GROUPS.map(group => (
        group.isDropdown ? (
          <TabGroup 
            key={group.id}
            group={group}
            isOpen={openDropdown === group.id}
            onToggle={() => setOpenDropdown(
              openDropdown === group.id ? null : group.id
            )}
          />
        ) : (
          <TabItem 
            key={group.tabs[0]} 
            tab={group.tabs[0]} 
          />
        )
      ))}
    </nav>
  );
};
```

---

### 3. 변경 필요 파일

| 파일 | 변경 내용 | 난이도 |
|------|----------|--------|
| `types.ts` | TabGroup, TAB_META 추가 | 하 |
| `App.tsx` | 탭 네비게이션 로직 교체 | 상 |
| `components/Navigation/` | 신규 컴포넌트 생성 | 중 |
| `components/Settings/SettingsModal.tsx` | 탭 권한 UI 업데이트 | 중 |
| `hooks/useTabAccess.ts` | 신규 훅 (권한 체크) | 중 |

---

## 🎨 UI/UX 상세

### 드롭다운 동작
1. **호버**: 데스크톱에서 마우스 호버 시 드롭다운 열림
2. **클릭**: 모바일/터치에서 클릭 시 토글
3. **외부 클릭**: 드롭다운 외부 클릭 시 닫힘
4. **키보드**: Escape 키로 닫기

### 활성 상태 표시
```
[📅 일정]  [📚 수업 ▼]  [👥 학생]  [⚙️ 관리]
             ↑ 현재 출석부 선택 시 그룹 강조
```

### 반응형 대응

| 화면 너비 | 표시 방식 |
|-----------|----------|
| ≥1200px | 전체 그룹 + 라벨 |
| 768-1199px | 전체 그룹 + 아이콘만 |
| <768px | 햄버거 메뉴 → 사이드 패널 |

---

## 📅 구현 일정

### Week 1: 기반 작업

| 일차 | 작업 내용 |
|------|----------|
| Day 1 | `types.ts` 데이터 구조 변경 |
| Day 2 | `NavigationBar.tsx`, `TabGroup.tsx` 생성 |
| Day 3 | `App.tsx` 네비게이션 교체 |
| Day 4-5 | 드롭다운 동작, 활성 상태 처리 |

### Week 2: 마무리

| 일차 | 작업 내용 |
|------|----------|
| Day 1-2 | 권한 시스템 적용 |
| Day 3 | 반응형 대응 |
| Day 4 | 설정 모달 업데이트 |
| Day 5 | 테스트 및 버그 수정 |

---

## ⚠️ 고려사항

### 1. 권한 시스템 호환
- 기존 `tabPermissions` 그대로 사용
- 그룹 내 접근 가능한 탭만 표시
- 그룹 내 모든 탭 접근 불가 시 그룹 숨김

### 2. URL 라우팅
- 현재: `?tab=attendance`
- 변경 없음 (탭 ID 유지)

### 3. 키보드 접근성
- Tab 키로 그룹 간 이동
- Enter/Space로 드롭다운 열기
- 화살표 키로 드롭다운 내 이동

---

## ✅ 완료 조건

- [ ] 그룹화된 드롭다운 정상 동작
- [ ] 기존 권한 시스템 호환
- [ ] 반응형 (모바일/태블릿/데스크톱)
- [ ] 키보드 접근성
- [ ] 기존 URL 파라미터 호환
- [ ] 설정 모달 권한 UI 업데이트

---

## 📊 예상 효과

| 지표 | 현재 | 개선 후 |
|------|------|---------|
| 최대 탭 수 | 7-8개 (한계) | 20개+ 가능 |
| 화면 사용 | 탭 전체 노출 | 4개 그룹으로 압축 |
| 확장성 | 낮음 | 높음 |
| 모바일 대응 | 미흡 | 햄버거 메뉴 지원 |

---

## 📞 다음 단계

1. **본 계획서 검토 및 승인**
2. Week 1 개발 착수
3. 중간 리뷰 후 Week 2 진행

**승인 시 개발 시작 가능합니다.**
