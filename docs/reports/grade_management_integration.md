# GradeGuard → IJW-Calander 성적 관리 탭 통합 계획서

> **작성일**: 2026-01-08
> **우선순위**: P2 (중간)
> **예상 소요**: 3-4주
> **담당**: 개발팀

---

## 📋 Executive Summary

### 목적
GradeGuard 프로젝트를 IJW-Calander의 **성적 관리** 탭으로 통합

### 주요 작업
1. GradeGuard 구성 요소 마이그레이션
2. Firebase Firestore 연동
3. IJW-Calander 디자인 시스템 적용
4. 기존 학생 데이터(students 컬렉션)와 연결

---

## 🎯 현황 분석

### GradeGuard 현재 구조

```
gradeguard/
├── App.tsx (525줄) - 메인 대시보드
├── types.ts - 타입 정의
├── components/
│   ├── Charts.tsx - 성적 추이 차트
│   ├── Sidebar.tsx - 사이드바 네비게이션
│   └── StatCard.tsx - 통계 카드
└── services/
    └── mockData.ts - 목업 데이터
```

### GradeGuard 데이터 타입

| 타입 | 필드 | 설명 |
|------|------|------|
| `Student` | id, name, school, gradeLevel, className, attendanceRate | 학생 기본정보 |
| `Exam` | id, title, date, type (MIDTERM/FINAL/MOCK/WEEKLY) | 시험 정보 |
| `Score` | examId, subject, score, average, maxScore, grade | 개별 점수 |
| `StudentStats` | student, scores[], currentGpa, rankInClass | 학생 통계 |
| `UpcomingEvent` | id, title, date, subject, dDay | 다가오는 시험 |

### IJW-Calander 현재 탭 구조

| TabIndex | 탭 이름 | 상태 |
|----------|---------|------|
| 0 | 연간 일정 | ✅ 완성 |
| 1 | 시간표 | ✅ 완성 |
| 2 | 출석부 | ✅ 완성 |
| 3 | 간트 차트 | ✅ 완성 |
| 4 | 상담 관리 | ✅ 완성 |
| 5 | 학생 관리 | ✅ 완성 |
| **6** | **성적 관리** (신규) | ⏳ 계획 중 |

---

## 🗄️ Firebase 데이터 설계

### 신규 컬렉션

#### 1. `exams` - 시험 정보
```typescript
interface Exam {
  id: string;
  title: string;           // "2학기 기말고사"
  date: string;            // "2026-01-20"
  type: 'midterm' | 'final' | 'mock' | 'weekly' | 'unit';
  subject: 'math' | 'english' | 'korean' | 'science' | 'all';
  gradeLevel?: string;     // "고1", "중2" 등
  createdAt: string;
  updatedAt: string;
}
```

#### 2. `scores` - 점수 기록
```typescript
interface Score {
  id: string;
  studentId: string;       // students 컬렉션 참조
  examId: string;          // exams 컬렉션 참조
  subject: 'math' | 'english' | 'korean' | 'science';
  score: number;           // 0-100
  maxScore: number;        // 기본 100
  grade?: 'A' | 'B' | 'C' | 'D' | 'F';
  rank?: number;
  note?: string;
  createdAt: string;
  updatedAt: string;
}
```

### 기존 컬렉션 활용

#### `students` - 기존 통합 학생 DB
```typescript
// 추가 필드 (선택적)
interface UnifiedStudent {
  // ... 기존 필드
  gradeStats?: {
    currentGpa?: number;
    rankInClass?: number;
    totalStudentsInClass?: number;
  }
}
```

---

## 🎨 디자인 일관성 가이드

### IJW-Calander 디자인 시스템

| 요소 | 값 | 적용 위치 |
|------|-----|----------|
| 헤더 배경 | `#081429` (Dark Navy) | 상단 네비게이션 |
| 강조색 | `#fdb813` (Gold) | 버튼, 선택 상태 |
| 카드 배경 | `white` + `rounded-lg shadow-sm` | 통계 카드 |
| 텍스트 | `#373d41` (Dark Gray) | 본문 |
| 보조 텍스트 | `text-gray-500` | 부가 정보 |

### 수정 필요 컴포넌트

| GradeGuard 컴포넌트 | 수정 사항 |
|--------------------|----------|
| `Sidebar.tsx` | 제거 (IJW 기존 탭 시스템 사용) |
| `StatCard.tsx` | 색상/그림자 IJW 스타일로 변경 |
| `Charts.tsx` | 차트 색상 IJW 팔레트 적용 |
| `App.tsx` | 분할하여 `GradeManagement/` 폴더로 이동 |

---

## 🏗️ 구현 계획

### Phase 1: 기반 구축 (Week 1)

#### 1.1 폴더 구조 생성
```
components/GradeManagement/
├── GradeManagementTab.tsx    # 메인 탭 컴포넌트
├── components/
│   ├── ScoreTable.tsx        # 성적표 테이블
│   ├── GradeChart.tsx        # 성적 추이 차트
│   ├── StudentScoreCard.tsx  # 학생 성적 카드
│   └── ExamManager.tsx       # 시험 관리
├── hooks/
│   ├── useExams.ts           # 시험 데이터 훅
│   └── useScores.ts          # 점수 데이터 훅
└── types.ts                  # 타입 정의
```

#### 1.2 Firebase 컬렉션 생성
- [ ] `exams` 컬렉션 생성
- [ ] `scores` 컬렉션 생성
- [ ] Firestore 보안 규칙 업데이트

#### 1.3 기본 훅 구현
- [ ] `useExams()` - 시험 CRUD
- [ ] `useScores()` - 점수 CRUD
- [ ] `useStudentGrades()` - 학생별 성적 조회

---

### Phase 2: UI 마이그레이션 (Week 2)

#### 2.1 메인 탭 구현
- [ ] `GradeManagementTab.tsx` 생성
- [ ] 학생 선택 드롭다운
- [ ] 과목별 필터

#### 2.2 성적표 테이블
- [ ] 시험별 점수 표시
- [ ] 평균/석차 표시
- [ ] 점수 입력/수정 모달

#### 2.3 차트 컴포넌트
- [ ] 성적 추이 그래프 (Recharts)
- [ ] 과목별 레이더 차트

#### 2.4 디자인 적용
- [ ] IJW 색상 팔레트 적용
- [ ] 기존 컴포넌트 스타일 일치

---

### Phase 3: 기능 완성 (Week 3)

#### 3.1 시험 관리
- [ ] 시험 추가/수정/삭제
- [ ] 시험 유형별 분류

#### 3.2 점수 입력
- [ ] 개별 점수 입력
- [ ] 대량 입력 (엑셀 붙여넣기)
- [ ] 자동 등급 계산

#### 3.3 통계 대시보드
- [ ] 학생별 GPA 계산
- [ ] 반 석차 산출
- [ ] 학교별 평균 비교

---

### Phase 4: 통합 및 테스트 (Week 4)

#### 4.1 기존 데이터 연동
- [ ] `students` 컬렉션과 연결
- [ ] 학생 관리 탭에서 성적 조회 링크

#### 4.2 권한 설정
- [ ] 성적 조회 권한
- [ ] 성적 입력 권한 (선생님만)

#### 4.3 테스트
- [ ] 단위 테스트
- [ ] 통합 테스트
- [ ] 사용자 테스트

---

## 📁 마이그레이션 상세

### 파일 매핑

| GradeGuard | → | IJW-Calander |
|------------|---|--------------|
| `App.tsx` | → | `GradeManagement/GradeManagementTab.tsx` |
| `types.ts` | → | `types.ts` (기존 파일에 타입 추가) |
| `components/Charts.tsx` | → | `GradeManagement/components/GradeChart.tsx` |
| `components/StatCard.tsx` | → | `GradeManagement/components/StatCard.tsx` |
| `components/Sidebar.tsx` | → | ❌ 제거 (IJW 탭 시스템 사용) |
| `services/mockData.ts` | → | ❌ 제거 (Firebase 연동) |

### 의존성 추가 불필요
- `recharts`: 이미 설치됨 (✅)
- `lucide-react`: 이미 설치됨 (✅)

---

## ⚠️ 위험 요소 및 대응

### 1. Firebase 비용 증가
**위험**: 성적 데이터 조회로 읽기 비용 증가
**대응**:
- React Query 캐싱 (5분)
- 필요한 데이터만 선별 로드
- 페이지네이션 적용

### 2. 디자인 불일치
**위험**: 마이그레이션 시 스타일 차이
**대응**:
- 철저한 디자인 리뷰
- Tailwind 클래스 일관성 유지
- 기존 컴포넌트 재사용

### 3. 데이터 정합성
**위험**: 학생 ID 불일치, 중복 데이터
**대응**:
- `students` 컬렉션 ID 참조
- 데이터 검증 로직 추가

---

## 📊 성공 지표

- [ ] 성적 입력 시간 < 5초/건
- [ ] 차트 렌더링 < 1초
- [ ] 디자인 일관성 90% 이상
- [ ] Firebase 읽기 증가 < 20%

---

## ✅ 체크리스트

### 사전 준비
- [x] GradeGuard 코드 분석
- [x] IJW-Calander 구조 확인
- [x] 데이터 모델 설계
- [ ] 디자인 가이드 확정

### 개발
- [ ] Firebase 컬렉션 생성
- [ ] 훅 구현
- [ ] UI 컴포넌트 마이그레이션
- [ ] 디자인 적용

### 배포
- [ ] 테스트 완료
- [ ] 코드 리뷰
- [ ] 프로덕션 배포
- [ ] 사용자 안내

---

## 📞 다음 단계

1. **본 계획서 검토 및 승인**
2. Firebase 컬렉션 생성
3. Phase 1 개발 착수

**예상 완료일**: 2026-02-05
