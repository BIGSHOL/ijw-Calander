# 📊 대형 파일 분리 최적화 계획서

> 작성일: 2026-01-05  
> 프로젝트: ijw-calander  
> 목표: 1000줄 이상 대형 파일 분리를 통한 유지보수성 향상

---

## 📋 현황 분석

### 대형 파일 목록 (1000줄 이상)

| 순위 | 파일명 | 행 수 | 경로 |
|:---:|--------|------:|------|
| 1 | App.tsx | **2,143** | `./App.tsx` |
| 2 | EnglishClassTab.tsx | **1,568** | `./components/Timetable/English/` |
| 3 | TimetableManager.tsx | **1,523** | `./components/Timetable/` |
| 4 | SettingsModal.tsx | **1,386** | `./components/` |
| 5 | EnglishTeacherTab.tsx | **1,076** | `./components/Timetable/English/` |
| 6 | EventModal.tsx | **1,000** | `./components/` |

**📊 총합: 8,696줄**

---

## ⚠️ 최우선 원칙

> **기능 및 성능 유지 원칙**
>
> 파일 분리 최적화는 기능이 느려지거나 축소되면 의미가 없습니다.  
> 모든 리팩토링은 **현재 기능을 그대로 유지**하면서 진행합니다.

---

## 🔧 파일별 분리 계획

### 1. App.tsx (2,143줄) → 목표: ~400줄

**현재 문제점:**
- 전체 앱 상태 관리 (40+ 상태 변수)
- 모든 핸들러 함수 포함 (20+ 핸들러)
- Firebase 구독 로직 집중 (5개 onSnapshot)
- UI 렌더링 + 비즈니스 로직 혼재

**분리 계획:**

```
📦 hooks/
├─ useAuth.ts                 // 인증 상태 관리
├─ useEvents.ts               // 일정 CRUD + 구독
├─ useBucketItems.ts          // 버킷리스트 관리
├─ useTaskMemos.ts            // 업무 메모 관리
└─ useAppMode.ts              // 탭/모드 관리

📦 utils/
├─ eventHandlers.ts           // 일정 핸들러 로직
├─ bucketHandlers.ts          // 버킷 핸들러 로직
└─ attendanceUtils.ts         // 참석 관리 유틸

📦 components/
├─ Header/
│   ├─ HeaderBar.tsx          // 상단 헤더
│   ├─ ProfileDropdown.tsx    // 프로필 메뉴
│   └─ MemoDropdown.tsx       // 메모 드롭다운
└─ Sidebar/
    └─ DepartmentList.tsx     // 부서 목록 사이드바
```

**예상 결과:** 2,143줄 → ~400줄 (↓81%)

---

### 2. EnglishClassTab.tsx (1,568줄) → 목표: ~300줄

**현재 문제점:**
- 복잡한 학생 통계 집계 로직
- 인라인 서브 컴포넌트 (ClassCard, MiniGridRow)
- 5개 대형 useEffect
- 복잡한 useMemo 변환 로직

**분리 계획:**

```
📦 hooks/english/
├─ useEnglishSettings.ts      // 설정 관리
├─ useEnglishLevels.ts        // 레벨 설정
├─ useStudentStats.ts         // 학생 통계 (최우선)
└─ useClassTransform.ts       // 데이터 변환

📦 utils/english/
├─ roomFormatter.ts           // formatRoomByDay
├─ teacherResolver.ts         // 담임 결정 로직
├─ scheduleLogic.ts           // Weekend shift 계산
└─ classGrouping.ts           // 그룹화 로직

📦 components/English/
├─ ClassCard.tsx              // 수업 카드 (독립 파일)
├─ MiniGridRow.tsx            // 미니 그리드 행
└─ StudentList.tsx            // 학생 목록 컴포넌트
```

**예상 결과:** 1,568줄 → ~300줄 (↓81%)

---

### 3. TimetableManager.tsx (1,523줄) → 목표: ~200줄

**현재 문제점:**
- 수학/영어 로직 혼재
- 40+ 상태 변수
- 복잡한 rowspan 계산 로직
- 긴 렌더링 JSX

**분리 계획:**

```
📦 components/Timetable/
├─ MathTimetable/
│   ├─ MathTimetable.tsx      // 수학 전용 탭
│   ├─ MathCell.tsx           // 수학 셀 컴포넌트
│   └─ MathStudentModal.tsx   // 학생 모달
└─ shared/
    ├─ PeriodHeader.tsx       // 교시 헤더
    ├─ WeekSelector.tsx       // 주 선택기
    └─ TimetableTable.tsx     // 공통 테이블

📦 hooks/timetable/
├─ useMathClasses.ts          // 수학 수업 데이터
├─ useTimetableConfig.ts      // 시간표 설정
└─ useRowspanCalculation.ts   // rowspan 계산
```

**예상 결과:** 1,523줄 → ~200줄 (↓87%)

---

### 4. SettingsModal.tsx (1,386줄) → 목표: ~150줄

**현재 문제점:**
- 11개 서브 탭이 단일 컴포넌트에
- 165줄 인라인 renderUserDetail 함수
- 복잡한 배치 저장 로직

**분리 계획:**

```
📦 components/settings/
├─ index.ts                   // 이미 존재
├─ UserDetailModal.tsx        // 사용자 상세 모달 (신규)
├─ CategoryManager.tsx        // 카테고리 관리 (신규)
└─ BatchSaveLogic.ts          // 저장 로직 분리

📦 hooks/settings/
├─ useBatchSave.ts            // 배치 저장 훅
└─ useSettingsState.ts        // 설정 상태 관리
```

**예상 결과:** 1,386줄 → ~150줄 (↓89%)

---

### 5. EnglishTeacherTab.tsx (1,076줄) → 목표: ~200줄

**현재 문제점:**
- 복잡한 드래그 선택 로직 (129줄)
- 셀 이동 로직 (281줄)
- 3가지 모드 전환 (view/edit/move)

**분리 계획:**

```
📦 hooks/english/
├─ useCellDrag.ts             // 드래그 선택 로직
├─ useCellMove.ts             // 셀 이동 로직
├─ useBatchInput.ts           // 일괄 입력 로직
└─ useTeacherSchedule.ts      // 스케줄 관리

📦 utils/english/
├─ dragUtils.ts               // 드래그 유틸
├─ moveUtils.ts               // 이동 유틸
└─ scheduleParser.ts          // 스케줄 파싱
```

**예상 결과:** 1,076줄 → ~200줄 (↓81%)

---

### 6. EventModal.tsx (1,000줄) → 목표: ~200줄

**현재 문제점:**
- 151줄 초기화 useEffect
- 24개 분리된 폼 상태 변수
- 복잡한 참가자 드롭다운 로직 (134줄)

**분리 계획:**

```
📦 hooks/events/
├─ useEventForm.ts            // 통합 폼 상태 훅
├─ useEventInit.ts            // 초기화 로직
└─ useParticipants.ts         // 참가자 관리

📦 components/EventModal/
├─ EventForm.tsx              // 폼 영역
├─ ParticipantDropdown.tsx    // 참가자 드롭다운
├─ ColorPicker.tsx            // 색상 선택
├─ RecurrenceOptions.tsx      // 반복 설정
└─ AttendanceList.tsx         // 참석 목록
```

**예상 결과:** 1,000줄 → ~200줄 (↓80%)

---

## 📈 예상 효과

| 지표 | 현재 | 목표 | 개선율 |
|------|-----:|-----:|-------:|
| 총 코드 행 | 8,696줄 | ~1,450줄 | **↓83%** |
| 평균 파일 크기 | 1,449줄 | ~241줄 | **↓83%** |
| 최대 파일 크기 | 2,143줄 | ~400줄 | **↓81%** |

---

## 🗓️ 실행 일정

| 단계 | 대상 파일 | 예상 기간 | 우선순위 |
|:---:|----------|:--------:|:-------:|
| 1단계 | EventModal.tsx | 2-3일 | 🔴 높음 |
| 2단계 | SettingsModal.tsx | 2-3일 | 🔴 높음 |
| 3단계 | EnglishTeacherTab.tsx | 3-4일 | 🟡 중간 |
| 4단계 | EnglishClassTab.tsx | 4-5일 | 🟡 중간 |
| 5단계 | TimetableManager.tsx | 5-7일 | 🟡 중간 |
| 6단계 | App.tsx | 7-10일 | 🟢 낮음 |

**총 예상 기간: 4-5주**

---

## ✅ 검증 체크리스트

각 파일 분리 후 반드시 확인:

- [ ] 모든 기존 기능 정상 작동
- [ ] UI 렌더링 속도 저하 없음
- [ ] 상태 관리 정상 동작
- [ ] Firebase 연동 정상
- [ ] 권한 체크 정상
- [ ] 반응형 레이아웃 유지

---

## 📝 참고 문서

- [Firebase 비용 최적화 Part 1](./firebase_cost_optimization_part1_현황분석.md)
- [Firebase 비용 최적화 Part 2](./firebase_cost_optimization_part2_구현가이드.md)
