# 인재원 학원 관리 시스템 (IJW Calendar)

> Firebase 기반 학원 통합 관리 플랫폼 - 시간표, 출석, 상담, 수납, 학생 관리 올인원 솔루션

![TypeScript](https://img.shields.io/badge/TypeScript-5.8-blue)
![React](https://img.shields.io/badge/React-19.2-61dafb)
![Firebase](https://img.shields.io/badge/Firebase-12.7-orange)
![Vite](https://img.shields.io/badge/Vite-6.2-646cff)

## 목차

- [프로젝트 소개](#프로젝트-소개)
- [주요 기능](#주요-기능)
- [기술 스택](#기술-스택)
- [시작하기](#시작하기)
- [환경 변수 설정](#환경-변수-설정)
- [프로젝트 구조](#프로젝트-구조)
- [주요 커스텀 훅](#주요-커스텀-훅)
- [권한 시스템](#권한-시스템)
- [개발 가이드](#개발-가이드)
- [배포](#배포)
- [문서](#문서)

---

## 프로젝트 소개

인재원 학원 관리 시스템은 학원 운영에 필요한 모든 기능을 하나의 플랫폼에서 제공하는 웹 기반 SaaS 솔루션입니다.

### 핵심 목표

- **효율성**: 반복 작업 자동화로 업무 시간 70% 절감
- **정확성**: 실시간 동기화로 데이터 불일치 제거
- **접근성**: 웹 기반으로 언제 어디서나 접근 가능
- **확장성**: 모듈식 설계로 새로운 기능 추가 용이

### 주요 사용자

- **원장/관리자**: 전체 학원 운영 관리
- **강사**: 출석/성적 관리, 시간표 확인
- **상담 담당자**: 학생 상담 기록 관리
- **회계 담당자**: 수납 및 급여 정산

---

## 주요 기능

### 1. 대시보드
- 실시간 KPI 모니터링 (재원생, 출석률, 수납률)
- 상담 현황 및 성과 추이 분석
- 빠른 작업 바로가기

### 2. 연간 일정 관리
- 다단 비교 뷰 (최대 3년 동시 표시)
- 드래그 앤 드롭으로 일정 이동
- 부서별 필터링 및 즐겨찾기
- 반복 일정 및 다중 부서 일정 지원

### 3. 시간표 관리
- **수학/영어 과목별 시간표**
- 통합 뷰로 전체 시간표 조망
- 반 이동 시뮬레이션 (레벨업/다운)
- 강의실 및 강사 배정 관리
- 엑셀 데이터 임포트

### 4. 출석 관리
- 월별/세션별 출석 체크
- 급여 자동 계산 (시급제/월급제)
- 일괄 처리 및 엑셀 내보내기
- 강사별 출석 현황 조회

### 5. 학생 관리
- 통합 학생 데이터베이스
- 재원/퇴원/예비생 상태 관리
- 과목별 수강 이력 (enrollments)
- 중복 학생 병합 도구
- 엑셀 일괄 등록

### 6. 등록 상담
- 상담 기록 및 진행 상태 추적
- 예비생 → 재원생 전환 프로세스
- 상담 통계 및 전환율 분석
- 월별 상담 목표 대비 현황

### 7. 수납 관리
- 학생별 수납 내역 관리
- 미수금 및 납부 예정 추적
- 월별 수납 현황 리포트

### 8. 성적 관리
- 학생별 성적 입력 및 조회
- 과목별 성적 추이 분석
- 성적표 자동 생성

### 9. 강사 관리
- 강사 정보 및 일정 관리
- 휴가 및 대체 강사 관리
- 강사별 수업 배정 현황

### 10. 교실 배정
- 시간대별 교실 사용 현황
- 자동 배정 알고리즘
- 충돌 감지 및 해결 제안

### 11. 간트 차트
- 프로젝트 일정 시각화
- 부서별 업무 계획 관리
- 캘린더 연동

### 12. 권한 관리 (RBAC)
- 역할 기반 접근 제어
- 세분화된 권한 설정 (읽기/쓰기/삭제)
- 역할 시뮬레이션 모드 (마스터 전용)

---

## 기술 스택

### Frontend
- **React 19.2** - UI 라이브러리
- **TypeScript 5.8** - 타입 안전성
- **Vite 6.2** - 빌드 도구
- **Tailwind CSS 3.4** - 스타일링
- **React Query (@tanstack/react-query)** - 서버 상태 관리
- **date-fns** - 날짜 처리
- **lucide-react** - 아이콘

### Backend
- **Firebase 12.7**
  - Firestore - NoSQL 데이터베이스
  - Authentication - 사용자 인증
  - Storage - 파일 저장 (미래)
- **Gemini AI** - AI 어시스턴트 (선택)

### UI/UX
- **@dnd-kit** - 드래그 앤 드롭
- **recharts** - 차트 시각화
- **html2canvas / jspdf** - PDF 생성
- **xlsx** - 엑셀 임포트/익스포트

### DevOps
- **ESLint** - 코드 린팅
- **Vitest** - 유닛 테스트
- **GitHub Actions** - CI/CD (설정 필요)

---

## 시작하기

### 사전 요구사항

```bash
Node.js >= 18.0.0
npm >= 9.0.0
```

### 설치

1. **저장소 클론**
```bash
git clone https://github.com/your-org/ijw-calander.git
cd ijw-calander
```

2. **의존성 설치**
```bash
npm install
```

3. **환경 변수 설정**
```bash
cp .env.example .env.local
```

`.env.local` 파일을 열어 아래 값을 설정하세요.

4. **개발 서버 실행**
```bash
npm run dev
```

서버가 `http://localhost:5173`에서 실행됩니다.

---

## 환경 변수 설정

`.env.local` 파일에 다음 환경 변수를 설정해야 합니다.

### 필수 변수

```env
# Firebase 프로젝트 설정 (Firebase Console에서 확인)
VITE_FIREBASE_API_KEY=your_firebase_api_key
VITE_FIREBASE_AUTH_DOMAIN=your_project_id.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your_project_id
VITE_FIREBASE_STORAGE_BUCKET=your_project_id.firebasestorage.app
VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
VITE_FIREBASE_APP_ID=your_app_id

# 암호화 키 (개인정보 암호화에 사용, 최소 32자 권장)
VITE_ENCRYPTION_KEY=your_256bit_encryption_key_here

# 마스터 계정 이메일 (최고 권한)
VITE_MASTER_EMAILS=admin@example.com
```

### 선택 변수

```env
# Gemini AI API 키 (AI 어시스턴트 기능 사용 시)
VITE_GEMINI_API_KEY=your_gemini_api_key

# 레거시 Firebase 프로젝트 (데이터 마이그레이션 시)
VITE_OLD_FIREBASE_PROJECT_ID=old_project_id
```

### 암호화 키 생성

```bash
# Node.js로 안전한 랜덤 키 생성
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

### 보안 주의사항

- `.env.local` 파일은 절대 Git에 커밋하지 마세요
- 암호화 키를 분실하면 기존 암호화된 데이터를 복구할 수 없습니다
- 프로덕션 환경에서는 환경 변수를 안전하게 관리하세요

---

## 프로젝트 구조

```
ijw-calander/
├── components/           # React 컴포넌트
│   ├── Attendance/       # 출석 관리
│   ├── Calendar/         # 연간 일정
│   ├── ClassManagement/  # 반 관리
│   ├── Classroom/        # 교실 배정
│   ├── Common/           # 공통 UI 컴포넌트
│   ├── Dashboard/        # 대시보드
│   ├── Gantt/            # 간트 차트
│   ├── Grades/           # 성적 관리
│   ├── Navigation/       # 사이드바/네비게이션
│   ├── RegistrationConsultation/  # 등록 상담
│   ├── Staff/            # 강사 관리
│   ├── StudentManagement/  # 학생 관리
│   ├── Timetable/        # 시간표
│   │   ├── Math/         # 수학 시간표
│   │   ├── English/      # 영어 시간표
│   │   └── Generic/      # 공통 시간표 컴포넌트
│   └── settings/         # 설정
├── hooks/                # 커스텀 React 훅
│   ├── useAuth.ts        # 인증
│   ├── useStudents.ts    # 학생 데이터
│   ├── useClasses.ts     # 반 데이터
│   ├── useAttendance.ts  # 출석 데이터
│   └── ...               # 기타 훅들
├── utils/                # 유틸리티 함수
│   ├── encryption.ts     # 암호화
│   ├── staffHelpers.ts   # 강사 헬퍼
│   ├── studentUtils.ts   # 학생 유틸
│   └── ...
├── types/                # TypeScript 타입 정의
│   ├── index.ts          # 타입 배럴 익스포트
│   ├── student.ts        # 학생 타입
│   ├── staff.ts          # 강사 타입
│   ├── attendance.ts     # 출석 타입
│   └── ...
├── contexts/             # React Context
├── scripts/              # 데이터 마이그레이션 스크립트
├── firebaseConfig.ts     # Firebase 초기화
├── App.tsx               # 메인 앱 컴포넌트
├── main.tsx              # 엔트리 포인트
└── vite.config.ts        # Vite 설정
```

---

## 주요 커스텀 훅

자세한 내용은 [docs/HOOKS.md](docs/HOOKS.md)를 참조하세요.

### 데이터 훅

- **useStudents** - 학생 목록 조회/수정
- **useClasses** - 반 목록 조회
- **useAttendance** - 출석 데이터 관리
- **useStaff** - 강사 정보 관리
- **useConsultations** - 상담 기록 관리

### 상태 관리 훅

- **useAuth** - 사용자 인증 상태
- **usePermissions** - 권한 체크
- **useAppState** - 앱 전역 상태

### 유틸리티 훅

- **useForm** - 폼 상태 관리
- **useFocusTrap** - 포커스 트랩 (접근성)
- **useGlobalSearch** - 전역 검색

---

## 권한 시스템

이 시스템은 **역할 기반 접근 제어 (RBAC)**를 사용합니다.

### 역할 계층

1. **master** - 최고 관리자 (모든 권한)
2. **admin** - 관리자 (거의 모든 권한)
3. **manager** - 매니저 (일부 관리 권한)
4. **math_lead** / **english_lead** - 과목 리더
5. **math_teacher** / **english_teacher** - 강사
6. **user** - 일반 사용자

### 권한 종류

- **탭 접근 권한**: 특정 탭(모드) 접근 가능 여부
- **데이터 권한**: 읽기/쓰기/삭제 권한
- **기능 권한**: 특정 기능 사용 가능 여부

### 권한 확인 방법

```typescript
import { usePermissions } from './hooks/usePermissions';

function MyComponent() {
  const { hasPermission } = usePermissions(userProfile);

  const canEdit = hasPermission('students.edit');
  const canDelete = hasPermission('students.delete');

  return (
    <>
      {canEdit && <EditButton />}
      {canDelete && <DeleteButton />}
    </>
  );
}
```

---

## 개발 가이드

### 코드 스타일

- **ESLint** 규칙 준수
- **TypeScript** strict 모드 사용
- **함수형 컴포넌트** 및 **Hooks** 사용
- **Tailwind CSS** 유틸리티 클래스 우선

### 커밋 메시지 규칙

```
feat: 새로운 기능 추가
fix: 버그 수정
docs: 문서 수정
style: 코드 포맷팅 (기능 변경 없음)
refactor: 코드 리팩토링
test: 테스트 추가/수정
chore: 빌드/설정 변경
```

예시:
```
feat(timetable): 영어 시간표 통합 뷰 추가
fix(attendance): 출석 중복 체크 버그 수정
docs: README에 설치 방법 추가
```

### 브랜치 전략

- **main** - 프로덕션 브랜치
- **develop** - 개발 브랜치
- **feature/xxx** - 기능 개발
- **fix/xxx** - 버그 수정

### 테스트

```bash
# 유닛 테스트 실행
npm run test

# UI 모드로 테스트
npm run test:ui

# 커버리지 리포트
npm run test:coverage
```

### 린팅

```bash
# 린팅 실행
npm run lint

# 자동 수정
npm run lint:fix
```

---

## 배포

### 빌드

```bash
npm run build
```

빌드된 파일은 `dist/` 폴더에 생성됩니다.

### 프리뷰

```bash
npm run preview
```

빌드된 앱을 로컬에서 미리 확인할 수 있습니다.

### 프로덕션 배포

1. **환경 변수 설정** - 프로덕션 환경에서 `.env.local` 대신 호스팅 플랫폼의 환경 변수 설정 사용
2. **Firestore 보안 규칙** - Firebase Console에서 적절한 보안 규칙 설정
3. **Firebase Hosting** (권장)
   ```bash
   npm install -g firebase-tools
   firebase login
   firebase init hosting
   firebase deploy
   ```

---

## 문서

- [아키텍처 문서](docs/ARCHITECTURE.md) - 시스템 구조 및 데이터 흐름
- [커스텀 훅 가이드](docs/HOOKS.md) - 주요 훅 사용법
- [기여 가이드](docs/CONTRIBUTING.md) - 개발 참여 방법

---

## 라이선스

이 프로젝트는 비공개 프로젝트입니다. 무단 복제 및 배포를 금지합니다.

---

## 제작

**인재원 개발팀**

문의: [support@example.com](mailto:support@example.com)

---

## 변경 이력

### v0.0.0 (개발 중)
- 초기 시스템 구축
- 주요 기능 모듈 개발 완료
- 권한 시스템 구현
- 성능 최적화 (React Query 도입)

---

## 트러블슈팅

### Firebase 연결 오류
```
Error: Missing required environment variables
```
**해결**: `.env.local` 파일의 Firebase 설정 확인

### 빌드 오류
```
Module not found: Can't resolve 'xxx'
```
**해결**: `npm install` 재실행, `node_modules` 삭제 후 재설치

### 권한 오류
```
Permission denied
```
**해결**: Firebase Console에서 Firestore 보안 규칙 확인, 사용자 권한 재설정

---

**Happy Coding!** 🚀
